---
name: obsidian-vault-organizer
description: Obsidian Vault 文件管理工作流 — 三层知识模型（L1知识点→L2疑问→L3项目）的归类、审计、scaffold。触发词：文件放哪/归类/整理目录/检查结构/建项目目录/新建笔记该放哪/记录/记一下/记到obsidian/记笔记
---

# Obsidian Vault Organizer

按三层知识模型管理 Obsidian Vault 中的所有文件。

## 三层模型

```
L1 原始知识点 → L2 疑问追问 → L3 项目应用组合
   (学什么)       (想通什么)       (做出什么)
```

**核心规则：**
- L1 知识按**技术栈/知识域**归位，不由"什么时候学的"决定
- L2 疑问物理上靠近产生它的 L1 知识（同一目录下的 `疑问.md` 或独立子目录）
- L3 项目通过 `[[wikilink]]` 链接回 L1/L2，不复制、不暂存知识
- L1 知识只在 `20-领域/编程/` 存一份，项目目录下不设 `技术知识点/` 缓冲区
- 附件/模板/工具类目录不参与三层模型

---

## 工作流 A：归类助手

用户问"这个文件应该放哪"时执行：

1. **判断层级**：是纯知识点（L1）、疑问/追问（L2）、还是项目应用笔记（L3）？
2. **L1 归类**：读取 `references/taxonomy.md`，按技术栈匹配到具体的 `01-15` 目录 + 子目录
3. **L2 归类**：找到对应的 L1 知识所在目录，建议在同一目录下创建或追加 `疑问.md`
4. **L3 归类**：确认属于哪个项目，放入 `10-项目/<项目名>/`
5. **输出**：目标路径 + 归类理由（一句话）

### 分类决策树

```
文件是纯知识点（可脱离项目独立存在）？
  ├── 是 → L1 → 查 taxonomy.md 按技术栈归类
  └── 否 → 是具体项目的实现/设计/工作日志？
            ├── 是 → L3 → 项目目录
            └── 否 → 是对某个知识点的疑问/追问？
                      ├── 是 → L2 → 靠近对应 L1 知识
                      └── 否 → 重新判断
```

---

## 工作流 B：结构审计

用户要求检查某个目录的文件结构时执行：

1. **扫描目标目录**：列出所有 .md 文件及其路径
2. **对照规则检查**：每个文件是否在正确的层级和目录
3. **报告问题**：按严重程度排序
   - 🔴 严重：L1 知识放在项目目录、L3 文件放在知识域
   - 🟡 一般：文件在正确的技术栈但子目录不对
   - 🔵 建议：可以合并的重复文件、空文件
4. **给出移动方案**：源路径 → 目标路径

**检查清单：**
- [ ] 项目目录（`10-项目/`）下是否有纯知识点文件？
- [ ] 知识域目录（`20-领域/编程/`）下是否有项目专属笔记？
- [ ] L1 文件是否在正确的技术栈分类下？
- [ ] Vault 根目录是否有散落文件？
- [ ] 是否有空文件或重复文件？

---

## 工作流 C：项目 Scaffold

用户要求为新项目建立目录骨架时执行：

1. 确认项目名称和所属分类
2. 在 `10-项目/` 下创建项目目录
3. 按 `references/project-scaffold.md` 创建子目录结构
4. 创建项目 MOC 文件（含 Dataview 查询模板）

---

## 配置

- **Vault 根路径**: 读取本目录下 `config.json` 的 `vault_root` 字段。首次使用请复制 `config.example.json` 为 `config.json` 并填入你自己的 Vault 路径。
- **L1 知识目录**: `20-领域/编程/`
- **L3 项目目录**: `10-项目/`
- **L2 位置规则**: 与对应 L1 知识同目录
- **附件目录**: `30-资源/媒体附件/`
- **模板目录**: `99-模板/`

---

## 写入规则（自动触发）

**当用户说"记录""记一下""记到 obsidian""记笔记"或任何要求把当前对话内容写入 Vault 的表述时，本 skill 应被自动调用并直接执行写入，无需用户再手动指定 obsidian-vault-organizer。** 默认写入此 Vault，不额外确认目标位置：
- 项目相关 → `10-项目/<项目>/`
- 知识点 → `20-领域/编程/<技术栈>/`
- 参考/工具 → `30-资源/工具/`
- 不确定时，按三层模型判断

执行时若目标 L1 主题已有对应笔记（如已存在 `PyTorch DataLoader 数据加载器.md`），应**补充进现有笔记**而非新建重复文件；纯疑问内容归为 L2，就近放在对应 L1 同目录。
