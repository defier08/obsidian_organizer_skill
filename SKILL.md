---
name: obsidian-vault-organizer
description: Obsidian Vault 文件管理工作流 — 三层知识模型（L1知识点→L2疑问→L3项目）的归类、审计、scaffold。自动扫描 Vault 目录结构生成分类索引，无需预置分类表，结构变化自动适配。触发词：文件放哪/归类/整理/检查结构/体检/清理/建项目目录/改笔记名/移动笔记；记录类指令（记录/记一下/记到obsidian/记笔记）归「写入规则」
---

# Obsidian Vault Organizer

按三层知识模型管理 Obsidian Vault 中的所有文件。**不依赖任何预置分类表**：每次运行自动扫描 Vault 的实际目录结构，按目录名提取关键词建立归类索引，随内容与结构变化增量适配。

## 设计原则：渐进式披露（先读）

成本与触发频率匹配，本 Skill 的指令文件只保留触发规则与流程摘要，详细脚本在 `scripts/`：

- **日常记录走轻量路径**：工作流 A/D 直接执行，只写目标笔记，不扫描全库、不读无关文件
- **批量整理手动触发**：工作流 E/F 是重活，用户说"整理/检查/清理/改名"才执行；一次性成本换长期干净，不随每次记录自动跑
- **归类默认按目录索引**，只有**可疑文件**才读内容判断。可疑 = 文件名不合命名规范 / 目录与内容疑似不符 / 空壳文件。判断优先用脚本提取头部摘要（`scripts/health-check.py --head`），可疑到必须确认时才全文 Read
- 涉及批量读写/链接替换时，一律写成脚本执行（模型只读汇总输出），不逐条人工处理

## 首次使用配置

本 Skill 需要配置后才能使用。首次使用时我会主动引导你完成配置。

配置项说明：
- `vault_root`（必填）：Obsidian Vault 根目录绝对路径
  示例：C:\Users\你的名字\Documents\MyVault
- `l1_root`（可选）：L1知识点目录，留空则自动扫描 Vault 结构探测
- `l3_root`（可选）：L3项目目录，留空则自动扫描探测
- `attachment_dir`（可选）：附件目录，留空则自动探测
- `template_dir`（可选）：模板目录，留空则自动探测

配置文件位置：`config.json`（与 SKILL.md 同目录）
修改配置：随时告诉我"重新配置 obsidian-vault-organizer"即可重新引导。
手动配置：复制 `config.example.json` 为 `config.json`，填入个人路径。

## 三层模型

```
L1 原始知识点 → L2 疑问追问 → L3 项目应用组合
   (学什么)       (想通什么)       (做出什么)
```

**核心规则：**
- L1 知识按**知识域**归位，不由"什么时候学的"决定
- L2 疑问物理上靠近产生它的 L1 知识（同一目录下，**一疑问一文件**：`技术域-疑问-主题.md`；同一知识的多次追问并入该疑问文件为章节，不新建多个文件）
- L3 项目通过 Obsidian 引用链接回 L1/L2（多种方式按需选择，见下），不复制、不暂存知识
- 附件/模板/工具类目录不参与三层模型；**归档与每日笔记目录（如 40-归档/、99-每日笔记/）同样不参与三层模型**，不移动、不归类；**工具/软件生成目录（如 copilot/、.agents/）通过 Obsidian ignore 排除，不移动文件**

### 引用方式选择（L3 → L1/L2）

Obsidian 支持多种引用方式，按场景按需选择，不要一律用同一种：

| 引用方式 | 语法 | 适用场景 |
|---|---|---|
| 普通维基链接 | `[[文件名]]` | 提到知识点、跳转查看，最常用 |
| 带别名 | `[[文件名\|显示文本]]` | 文件名不适合直接显示时（如 MOC 索引） |
| 带路径 | `[[路径/文件名]]` | 存在重名文件、需消除歧义时 |
| 嵌入引用 | `![[文件名]]` | 需在项目文档中展示知识内容（transclusion） |
| 链接到段落 | `[[文件名#标题]]` / `[[文件名#^块ID]]` | 引用知识文件中的特定章节或块 |
| 嵌入段落 | `![[文件名#标题]]` | 只嵌入知识文件中的特定段落 |

**选择原则：**
- 只是提到/索引知识点 → `[[文件名]]` 或 `[[文件名|别名]]`
- 需在项目文档中展示知识内容 → `![[文件名]]`（嵌入）
- 有重名风险 → 带路径 `[[路径/文件名]]`
- 只需引用/嵌入特定段落 → `[[文件名#标题]]` 或 `![[文件名#标题]]`

**语法边界**：上表是 L3→L1/L2 引用的**选择原则**，属于 Obsidian 语法的常用子集。完整语法（frontmatter、callout、标签、嵌入细节、注释等）以 `obsidian-markdown` skill 为准；本技能不重复维护语法大全，只负责引用方式选择与链接同步。

---

## 配置

- **Vault 根路径**: 读取本目录下 `config.json` 的 `vault_root` 字段。首次使用请复制 `config.example.json` 为 `config.json` 并填入自己的 Vault 路径。
- **可选覆盖**（`config.json` 中，留空则自动探测/使用默认值）：
  - `l1_root`: L1 知识根目录名（默认 `10-领域/`）
  - `l3_root`: L3 项目根目录名（默认 `20-项目/`）
  - `attachment_dir`: 附件目录名（默认 `30-资源/媒体附件/`）
  - `template_dir`: 模板目录名（默认 `99-模板/`）

---

## 自动分类索引（扫描驱动）

每次执行工作流 A/B/E 前：

1. **扫描**：递归列出 `vault_root` 下所有目录，以**目录名为关键词**建立「目录 ↔ 关键词」归类索引（如目录名含 "Vue" → Vue 相关笔记归入；含 "Git" → Git/GitHub 相关归入）。
2. **合并人工补充**：若 Vault 根存在 `_taxonomy.generated.md`，先合并其中的手工映射，再以本次扫描结果为准更新。
3. **落盘（可选）**：将合并结果写回 `_taxonomy.generated.md`，供审计与人工微调（该文件位于 Vault 内，不属于本 Skill，不随 Skill 分发）。

**执行主体说明**：以上扫描 / 合并 / 落盘均由 AI 按本规则在调用时现场执行（**运行态索引，无脚本持久化**）；`scripts/` 承担健康检查、链接验证与改名链接同步等确定性重活（`health-check.py` / `verify_links.py` / `rename_with_links.py`），不生成该索引。落盘仅为可选审计快照，非自动增量维护。

**兜底探测**：若扫描后找不到 L1 知识根（含最多知识笔记的顶层目录）或 L3 项目根（名字含"项目"的顶层目录），按 `config.json` 默认值建议创建。

---

## 命名规范（默认规则）

- 文件名 = **技术域-主题.md**（如 `Vue2-computed计算属性.md`、`GeoServer-入门.md`、`SQL-表连接.md`）
- **禁止**：无前缀散名（`$emit.md`）、泛化名（`index`/`basic`/`buildings`）、口语化名（`xxx到底是啥`）、版本后缀（`(1)`/`(2)`、`v2`/`v3`）作为文件主名
- 版本/进阶内容应**并入主文件为章节**，不拆成 `主题`+`主题进阶` 两个文件
- 项目实战笔记（代码解析、数据明细、工作日志）归 L3 项目目录，即使内容"用到了某个技术"
- 通用知识点放错技术目录时（如 Windows 路径知识在 Vue 目录），移回正确的知识域

---

## 工作流 A：归类助手

用户问"这个文件应该放哪"时执行：

1. **判断层级**：是纯知识点（L1）、疑问/追问（L2）、还是项目应用笔记（L3）？
2. **L1 归类**：按自动分类索引，用笔记内容关键词匹配 → 知识域子目录
3. **L2 归类**：找到对应 L1 知识所在目录，建议在同一目录下新建 `技术域-疑问-主题.md`（一疑问一文件，同一疑问的追问并入同文件章节，不合并多个疑问）
4. **L3 归类**：确认属于哪个项目，放入 L3 项目目录
5. **无匹配时**：以笔记核心主题为名**建议新建子目录**，征询确认后创建，并把「目录名 ↔ 关键词」回写到 `_taxonomy.generated.md`
6. **输出**：目标路径 + 归类理由（一句话）

### 分类决策树

```
文件名规范 + 目录位置合理？
  ├── 否 → 读头部摘要再判断（可疑文件才读，优先 --head 摘要）
  └── 是 → 按目录索引归类，不读内容
文件是纯知识点（可脱离项目独立存在）？
  ├── 是 → L1 → 按自动分类索引归类
  └── 否 → 是具体项目的实现/设计/工作日志？
            ├── 是 → L3 → 项目目录
            └── 否 → 是对某个知识点的疑问/追问？
                      ├── 是 → L2 → 靠近对应 L1 知识
                      └── 否 → 重新判断
```

---

## 工作流 B：结构审计

用户要求检查某个目录的文件结构时执行：

1. **扫描目标目录**：列出所有 .md 文件及其路径
2. **对照三层模型规则检查**：每个文件是否在正确的层级和目录
3. **报告问题**：按严重程度排序
   - 🔴 严重：L1 知识放在项目目录、L3 文件放在知识域
   - 🟡 一般：文件在正确的知识域但子目录不对、命名不合规范
   - 🔵 建议：可以合并的重复文件、空文件
4. **给出移动方案**：源路径 → 目标路径

**检查清单：**
- [ ] L3 项目目录下是否有纯知识点文件？
- [ ] L1 知识域目录下是否有项目专属笔记？
- [ ] L1 文件是否在正确的知识域分类下？
- [ ] Vault 根目录是否有散落文件？
- [ ] 是否有空文件或重复文件？
- [ ] 笔记是否都带 frontmatter（title/date/tags）？（缺的列为 🔵 建议补充）

目录较大（>20 篇）时，先用 `scripts/health-check.py --dir <目录>` 跑脚本体检，再针对报告决策，不逐个人工翻。

---

## 工作流 C：项目 Scaffold

用户要求为新项目建立目录骨架时执行：

1. 确认项目名称和所属分类
2. 在 L3 项目目录下创建项目目录
3. 按 `references/project-scaffold.md` 创建子目录结构（该文件为示例骨架，可按个人习惯增删）
4. 创建项目 MOC 文件（含 Dataview 查询模板）

---

## 工作流 D：待记录缓冲区（解决"连续问多个问题才说记录"的范围问题）

### 问题背景

用户可能连续问了好几个问题才说"记录"，此时助手无法确定该记哪几个。可能只记了最后一个，也可能全记导致混乱。

### 解决方案：待记录缓冲区

维护一个"待记录知识点缓冲区"，用户说"记录"时先列出可选列表，让用户选择。

### D.1 登记阶段（自动，无需用户操作）

**登记单位 = 用户的每次提问**。用户每提出一个问题并得到解答后，以该问题为单位自动登记到缓冲区（用户问了 N 个问题，缓冲区就有 N 项）：

登记项格式：
- 编号：自动递增
- 标题：**用户问题的简短复述**（以用户原话为锚点，不自由发挥）
- 摘要：该问题对应的核心结论（1-2句，忠实于解答内容）
- 目标笔记：预计写入哪个笔记文件（如已知）

**登记时机**：
- 用户每完成一轮"提问 → 解答"后
- 以用户问题为边界，一轮提问对应一项
- 不把一个问题拆成多个知识点，也不把多个问题合并成一项

**缓冲区存储**：默认会话内存中维护（新对话开始为空）；**跨会话需要保留时**，落盘到 Vault 内 `30-资源/待记录缓冲区.md`，下次会话读取续用。落盘格式与登记项一致，编号跨会话连续。

**收集范围界定（轮次窗口，N=10）**：
- 每个登记项自动记录产生时的**轮次号**（会话内从 1 递增）
- 记录 触发时，只列出**最近 N=10 轮内**登记的知识点；更早的默认不列（滑出窗口）
- 窗口外内容不丢：用户可用 D.4 的"记录刚才讲的X"补记
- 轮次从会话开始计算，新对话窗口清零

### D.2 触发记录（用户说"记录"时）

用户说"记录""记一下""记到obsidian"时，**不要直接写**，先执行：

1. 读取缓冲区（内存或落盘文件），列出所有待记录项：

```
你最近 10 轮问了 N 个问题：
1. [标题1] — [摘要]
2. [标题2] — [摘要]
3. [标题3] — [摘要]

要记录哪些？
- 回复"全部"：记录所有
- 回复编号（如"1,3"）：只记录指定项
- 回复"都不记"：清空缓冲区，不记录
```

2. 等待用户选择
3. 根据选择执行记录（按工作流A的归类规则）
4. 记录完成后，**清空已记录的项**，未记录的项保留在缓冲区

### D.3 缓冲区管理

| 用户指令 | 行为 |
|---------|------|
| "记录" / "记一下" | 列出缓冲区，等待选择 |
| "全部记录" | 直接记录所有，不需要再确认 |
| "只记第X个" / "记1,3" | 只记录指定编号 |
| "清空记录" / "不记了" | 清空缓冲区 |
| "记录刚才讲的X" | 直接补记指定内容（同 D.4） |
| "记录缓冲区有什么" | 列出当前缓冲区内容 |

### D.4 特殊情况

- **缓冲区为空时用户说"记录"**：直接记录当前正在讲的内容（退化为旧行为）
- **用户说"记录刚才讲的X"**：直接记录指定的内容，不需要列出缓冲区
- **记录后用户继续问问题**：新知识点继续登记到缓冲区

---

## 工作流 E：健康体检（手动触发）

触发词：**整理/体检/检查全库/清理/去重/发现重复**。执行：

1. 运行 `scripts/health-check.py`（Vault 路径自动读 config.json，或 `--vault <path>`）
2. 模型**只读脚本输出汇总**，逐项决策：
   - 重复文件名对 → 读头部摘要判断是否合并（工作流 F）
   - 无前缀/泛化名文件 → 按命名规范改名
   - 空壳文件（空正文）→ 删除或归档
   - 缺 frontmatter 的笔记 → 列为 🔵 建议补充属性头（低优先级，不自动改写）
   - 断链清单 → 区分误报（`Pasted image` 附件嵌入、学习路径占位符）与真断链
3. 需要判断内容时：`health-check.py --head <路径>` 提取头部摘要，可疑到必须确认才全文 Read
4. 执行合并/改名/移动后，**必须**跑 `scripts/verify_links.py` 验证，残留断链 = 0 才算完成
5. 用户没说"全部"时，**先列出体检报告 + 建议方案，等用户选择范围**再执行

---

## 工作流 F：改名与链接同步（三件套，手动触发）

改名 / 移动 / 删除文件必须按四步做，禁止只改名不同步链接：

0. **计划预览（必做）**：批量操作前先列出完整清单（源路径 → 目标路径，逐条），等待用户确认范围；用户没说"全部"时只处理确认的部分。**回滚保障**：Vault 为 git 仓库时先 commit 或打 tag（如 `git tag organizer-before-YYYYMMDD`），失败可回滚；非 git 仓库先对整个 Vault 做 zip 快照（失败可从快照恢复）
1. **改名/移动**：由 `scripts/rename_with_links.py` 批量执行（幂等：源不存在或目标已存在则跳过，防半途冲突）；删除前先确认内容已并入主文件
2. **链接重写**：由 `scripts/rename_with_links.py` 一并执行（改名 + 重写一体，规则已固化进脚本，模型不另起炉灶）：全 Vault 替换 `[[旧名` → `[[新名`；**长名先于短名替换**（如 `[[Vue2-computed计算属性` 先保护，再替换 `[[Vue2-compute`），避免互相污染。**同名文件必须按路径区分目标**，要点如下（完整实现见脚本）：
   - 存在同名文件（多个文件 basename 相同，如三个目录下都有 `疑问.md`）时，先按**完整路径**建立「旧路径/旧名 → 新名」逐文件映射，禁止用一个新名替换所有同名文件
   - 带路径的链接 `[[路径/旧名...]]`：**路径参与匹配**——只有路径指向的目标是该映射中的文件时才替换，且保留原路径、只换文件名（如 `[[03-Vue3/疑问]]` → `[[03-Vue3/Vue3-疑问记录]]`，绝不能替换成其他目录的新名）
   - 无路径裸链接 `[[旧名]]`：同名场景下有歧义（Obsidian 无法确定指向哪个），**先列出候选让用户确认指向哪个文件**，再写成带路径的新链接，不盲目替换
   - **完整目标名匹配**：旧名后面必须紧跟 `]`、`|`、`#` 才命中，防止旧名是其他文件名的子串时误伤（如 `[[数据增强疑问]]` 含"疑问"，必须排除）
   - **只改链接，不碰 frontmatter**：替换只作用于 `[[...]]` 内部；YAML 属性（tags/aliases 等）中与旧名同名的标签**不得替换**（此前整理曾误改 `tags: [疑问]`，属违规操作，需避免）
3. **验证**：`scripts/verify_links.py` 全库扫描，删除文件残留链接 = 0、新名链接全部有效，才算完成

合并内容时：把被合并文件的正文（去 frontmatter）追加到主文件末尾，加 `---\n## 附：<原标题>` 分隔，保证内容零丢失；正文中的代码块原样保留（见「写入格式规则」）；只有空壳才直接删除。

---

## 写入规则（自动触发）

**当用户说"记录""记一下""记到 obsidian""记笔记"或任何要求把当前对话内容写入 Vault 的表述时，本 skill 应被自动调用。**

**第一步：检查待记录缓冲区（工作流D）**
- 如果缓冲区有多个待记录项 → 先列出可选列表，让用户选择范围
- 如果缓冲区只有1项或为空 → 直接记录当前内容
- 如果用户明确说"全部记录"或"只记X" → 按用户指定执行，不需要再列列表

**第二步：按归类规则写入**
- 项目相关 → L3 项目目录 `<项目>/`
- 知识点 → L1 知识根 `<知识域>/`（按自动分类索引）
- 参考/工具 → 附件/工具目录
- 不确定时，按三层模型判断

执行时若目标 L1 主题已有对应笔记，应**补充进现有笔记**而非新建重复文件；纯疑问内容归为 L2，就近放在对应 L1 同目录。

**第三步（命名）**：新建文件按命名规范命名（技术域-主题.md）；目标目录已有同类笔记则合并而非新建。

### 写入格式规则：新笔记必须带 frontmatter 属性头（硬性要求）

新建任何笔记（L1 知识点、L2 疑问、L3 项目及 MOC 索引）都必须带 YAML frontmatter，位于文件最顶部：

```yaml
---
title: 笔记标题
date: YYYY-MM-DD
tags:
  - 知识域
aliases:
  - 便于检索的别名
---
```

- `title`：笔记标题，默认与文件名一致；文件名含技术域前缀时，title 可去掉前缀只留主题，按内容定
- `date`：创建日期，格式 `YYYY-MM-DD`
- `tags`：至少一个标签，优先用知识域名（如 `Vue`、`Git`、`SQL`），支持嵌套如 `Vue/组件`；**同一 Vault 内大小写/语言保持一致，嵌套不超过两级，不建零散口语标签**
- `aliases`：可选，让其他笔记通过别名检索/链接到本笔记；不确定时不填
- **仅新建文件时写 frontmatter**；追加合并现有笔记（工作流 F）时保留原 frontmatter，不重写、不覆盖
- 已有笔记缺 frontmatter 时，在工作流 B/E 中列为建议项（🔵），只提示不自动改写

### 写入格式规则：代码块必须保留为代码块（硬性要求）

记录任何含代码的内容时，**代码必须写成围栏代码块，禁止转成普通文本、缩进文本或行内代码**：

- 源内容中的围栏代码块（```` ``` ```` 或 `~~~` 包裹，含语言标注如 ```` ```python ````）→ **原样写入笔记**，保留围栏与语言标注，代码内容逐字保留，不改写、不省略
- 源内容中的命令行操作、终端输出、配置文件片段 → 写成带语言标注的围栏代码块（如 ```` ```bash ````、```` ```console ````、```` ```yaml ````）
- 单行命令或短代码片段：对话中明确是代码的，一律写成围栏代码块；只有对话中本来就是行内提及（如“用 `git log` 查看历史”）才保留为行内代码
- 笔记内需要展示“代码块语法”本身时，用四重反引号（```` ``` ````）包裹示例，避免与真实代码块冲突
- 追加合并现有笔记时（工作流 F）同样适用本规则

### 写入格式规则：可视化配图必须落盘并嵌入（硬性要求）

讲解/回答中若产出了可视化图（HTML/SVG 信息图、ECharts 图表、示意结构等），记录时**必须把图落盘为图片文件并嵌入笔记，禁止只记文字跳过配图**：

- **落盘位置**：`<attachment_dir>`（默认 `30-资源/媒体附件/`），与源 HTML 成对存放（`主题.png` + `主题.html`，后者保留可复改源）
- **嵌入方式**：在笔记中用 Obsidian 嵌入语法 `![[媒体附件/主题.png]]`（与 Vault 内 PPT 可视化笔记同款约定）
- **生成方式**：把渲染器 HTML 存为 `.html`，用无头浏览器截图为 PNG（如 Edge/Chrome `--headless --disable-gpu --screenshot=<png> --window-size=1280,<高> file:///...`），截图后目检完整性（内容不截断、无乱码）
- **失败降级**：截图失败或无法生成图片时，在笔记中注明"配图待补"，不得静默跳过
- 追加合并现有笔记时（工作流 F）同样适用本规则
