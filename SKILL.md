---
name: obsidian-vault-organizer
description: Obsidian Vault 文件管理工作流 — 三层知识模型（L1知识点→L2疑问→L3项目）的归类、审计、scaffold。自动扫描 Vault 目录结构生成分类索引，无需预置分类表，结构变化自动适配。触发词：文件放哪/归类/整理目录/检查结构/建项目目录/新建笔记该放哪/记录/记一下/记到obsidian/记笔记
---

# Obsidian Vault Organizer

按三层知识模型管理 Obsidian Vault 中的所有文件。**不依赖任何预置分类表**：每次运行自动扫描 Vault 的实际目录结构，按目录名提取关键词建立归类索引，随内容与结构变化增量适配。

## 首次使用配置

本 Skill 需要配置后才能使用。首次使用时我会主动引导你完成配置。

配置项说明：
- `vault_root`（必填）：Obsidian Vault 根目录绝对路径
  示例：C:\Users\你的名字\Documents\MyVault
- `l1_root`（可选）：L1知识点目录，留空则自动扫描 Vault 结构探测
- `l3_root`（可选）：L3项目目录，留空则自动扫描探测
- `attachment_dir`（可选）：附件目录，留空则自动探测
- `template_dir`（可选）：模板目录，留空则自动探测

配置文件位置：`config.json`（与 SKILL.md 同目录）
修改配置：随时告诉我"重新配置 obsidian-vault-organizer"即可重新引导。
手动配置：复制 `config.example.json` 为 `config.json`，填入个人路径。

## 三层模型

```
L1 原始知识点 → L2 疑问追问 → L3 项目应用组合
   (学什么)       (想通什么)       (做出什么)
```

**核心规则：**
- L1 知识按**知识域**归位，不由"什么时候学的"决定
- L2 疑问物理上靠近产生它的 L1 知识（同一目录下的 `疑问.md` 或独立子目录）
- L3 项目通过 Obsidian 引用链接回 L1/L2（多种方式按需选择，见下），不复制、不暂存知识
- 附件/模板/工具类目录不参与三层模型

### 引用方式选择（L3 → L1/L2）

Obsidian 支持多种引用方式，按场景按需选择，不要一律用同一种：

| 引用方式 | 语法 | 适用场景 |
|---|---|---|
| 普通维基链接 | `[[文件名]]` | 提到知识点、跳转查看，最常用 |
| 带别名 | `[[文件名\|显示文本]]` | 文件名不适合直接显示时（如 MOC 索引） |
| 带路径 | `[[路径/文件名]]` | 存在重名文件、需消除歧义时 |
| 嵌入引用 | `![[文件名]]` | 需在项目文档中展示知识内容（transclusion） |
| 链接到段落 | `[[文件名#标题]]` / `[[文件名#^块ID]]` | 引用知识文件中的特定章节或块 |
| 嵌入段落 | `![[文件名#标题]]` | 只嵌入知识文件中的特定段落 |

**选择原则：**
- 只是提到/索引知识点 → `[[文件名]]` 或 `[[文件名|别名]]`
- 需在项目文档中展示知识内容 → `![[文件名]]`（嵌入）
- 有重名风险 → 带路径 `[[路径/文件名]]`
- 只需引用/嵌入特定段落 → `[[文件名#标题]]` 或 `![[文件名#标题]]`

---

## 配置

- **Vault 根路径**: 读取本目录下 `config.json` 的 `vault_root` 字段。首次使用请复制 `config.example.json` 为 `config.json` 并填入自己的 Vault 路径。
- **可选覆盖**（`config.json` 中，留空则自动探测/使用默认值）：
  - `l1_root`: L1 知识根目录名（默认 `10-领域/`）
  - `l3_root`: L3 项目根目录名（默认 `20-项目/`）
  - `attachment_dir`: 附件目录名（默认 `30-资源/媒体附件/`）
  - `template_dir`: 模板目录名（默认 `99-模板/`）

---

## 自动分类索引（扫描驱动）

每次执行工作流 A/B 前：

1. **扫描**：递归列出 `vault_root` 下所有目录，以**目录名为关键词**建立「目录 ↔ 关键词」归类索引（如目录名含 "Vue" → Vue 相关笔记归入；含 "Git" → Git/GitHub 相关归入）。
2. **合并人工补充**：若 Vault 根存在 `_taxonomy.generated.md`，先合并其中的手工映射，再以本次扫描结果为准更新。
3. **落盘（可选）**：将合并结果写回 `_taxonomy.generated.md`，供审计与人工微调（该文件位于 Vault 内，不属于本 Skill，不随 Skill 分发）。

**兜底探测**：若扫描后找不到 L1 知识根（含最多知识笔记的顶层目录）或 L3 项目根（名字含"项目"的顶层目录），按 `config.json` 默认值建议创建。

---

## 工作流 A：归类助手

用户问"这个文件应该放哪"时执行：

1. **判断层级**：是纯知识点（L1）、疑问/追问（L2）、还是项目应用笔记（L3）？
2. **L1 归类**：按自动分类索引，用笔记内容关键词匹配 → 知识域子目录
3. **L2 归类**：找到对应 L1 知识所在目录，建议在同一目录下创建或追加 `疑问.md`
4. **L3 归类**：确认属于哪个项目，放入 L3 项目目录
5. **无匹配时**：以笔记核心主题为名**建议新建子目录**，征询确认后创建，并把「目录名 ↔ 关键词」回写到 `_taxonomy.generated.md`
6. **输出**：目标路径 + 归类理由（一句话）

### 分类决策树

```
文件是纯知识点（可脱离项目独立存在）？
  ├── 是 → L1 → 按自动分类索引归类
  └── 否 → 是具体项目的实现/设计/工作日志？
            ├── 是 → L3 → 项目目录
            └── 否 → 是对某个知识点的疑问/追问？
                      ├── 是 → L2 → 靠近对应 L1 知识
                      └── 否 → 重新判断
```

---

## 工作流 B：结构审计

用户要求检查某个目录的文件结构时执行：

1. **扫描目标目录**：列出所有 .md 文件及其路径
2. **对照三层模型规则检查**：每个文件是否在正确的层级和目录
3. **报告问题**：按严重程度排序
   - 🔴 严重：L1 知识放在项目目录、L3 文件放在知识域
   - 🟡 一般：文件在正确的知识域但子目录不对
   - 🔵 建议：可以合并的重复文件、空文件
4. **给出移动方案**：源路径 → 目标路径

**检查清单：**
- [ ] L3 项目目录下是否有纯知识点文件？
- [ ] L1 知识域目录下是否有项目专属笔记？
- [ ] L1 文件是否在正确的知识域分类下？
- [ ] Vault 根目录是否有散落文件？
- [ ] 是否有空文件或重复文件？

---

## 工作流 C：项目 Scaffold

用户要求为新项目建立目录骨架时执行：

1. 确认项目名称和所属分类
2. 在 L3 项目目录下创建项目目录
3. 按 `references/project-scaffold.md` 创建子目录结构（该文件为示例骨架，可按个人习惯增删）
4. 创建项目 MOC 文件（含 Dataview 查询模板）

---

## 工作流 D：待记录缓冲区（解决"连续问多个问题才说记录"的范围问题）

### 问题背景

用户可能连续问了好几个问题才说"记录"，此时助手无法确定该记哪几个。可能只记了最后一个，也可能全记导致混乱。

### 解决方案：待记录缓冲区

维护一个会话级的"待记录知识点缓冲区"，用户说"记录"时先列出可选列表，让用户选择。

### D.1 登记阶段（自动，无需用户操作）

每次讲完一个**独立的知识点**后，助手自动登记到缓冲区：

登记项格式：
- 编号：自动递增
- 标题：知识点的简短标题（如"Grad-CAM++数学原理"）
- 摘要：1-2句话概括内容
- 目标笔记：预计写入哪个笔记文件（如已知）

**登记时机**：
- 讲完一个完整的概念/原理/例子后
- 用户切换到新话题前
- 不是每句话都登记，只登记"独立的、值得记录的知识点"

**缓冲区存储**：会话内存中维护，不需要持久化文件。新对话开始时缓冲区为空。

**收集范围界定（轮次窗口，N=10）**：
- 每个登记项自动记录产生时的**轮次号**（会话内从 1 递增）
- 记录 触发时，只列出**最近 N=10 轮内**登记的知识点；更早的默认不列（滑出窗口）
- 窗口外内容不丢：用户可用 D.4 的“记录刚才讲的X”补记
- 轮次从会话开始计算，新对话窗口清零

### D.2 触发记录（用户说"记录"时）

用户说"记录""记一下""记到obsidian"时，**不要直接写**，先执行：

1. 读取缓冲区，列出所有待记录项：

```
你最近 10 轮讲了 N 个知识点：
1. [标题1] — [摘要]
2. [标题2] — [摘要]
3. [标题3] — [摘要]

要记录哪些？
- 回复"全部"：记录所有
- 回复编号（如"1,3"）：只记录指定项
- 回复"都不记"：清空缓冲区，不记录
```

2. 等待用户选择
3. 根据选择执行记录（按工作流A的归类规则）
4. 记录完成后，**清空已记录的项**，未记录的项保留在缓冲区

### D.3 缓冲区管理

| 用户指令 | 行为 |
|---------|------|
| "记录" / "记一下" | 列出缓冲区，等待选择 |
| "全部记录" | 直接记录所有，不需要再确认 |
| "只记第X个" / "记1,3" | 只记录指定编号 |
| "清空记录" / "不记了" | 清空缓冲区 |
| "记录刚才讲的X" | 直接补记指定内容（同 D.4） |
| "记录缓冲区有什么" | 列出当前缓冲区内容 |

### D.4 特殊情况

- **缓冲区为空时用户说"记录"**：直接记录当前正在讲的内容（退化为旧行为）
- **用户说"记录刚才讲的X"**：直接记录指定的内容，不需要列出缓冲区
- **记录后用户继续问问题**：新知识点继续登记到缓冲区

---

## 写入规则（自动触发）

**当用户说"记录""记一下""记到 obsidian""记笔记"或任何要求把当前对话内容写入 Vault 的表述时，本 skill 应被自动调用。**

**第一步：检查待记录缓冲区（工作流D）**
- 如果缓冲区有多个待记录项 → 先列出可选列表，让用户选择范围
- 如果缓冲区只有1项或为空 → 直接记录当前内容
- 如果用户明确说"全部记录"或"只记X" → 按用户指定执行，不需要再列列表

**第二步：按归类规则写入**
- 项目相关 → L3 项目目录 `<项目>/`
- 知识点 → L1 知识根 `<知识域>/`（按自动分类索引）
- 参考/工具 → 附件/工具目录
- 不确定时，按三层模型判断

执行时若目标 L1 主题已有对应笔记，应**补充进现有笔记**而非新建重复文件；纯疑问内容归为 L2，就近放在对应 L1 同目录。
